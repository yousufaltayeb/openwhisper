# Archived Flatpak packaging

This directory packages the pre-rewrite Python/Tauri application. It was
archived on 2026-08-20 and is not a release path for the Rust daemon/OpenTUI
application. Publication and source-preparation entry points intentionally stop
with an error so obsolete artifacts cannot be produced accidentally.
