{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimum permissions for the OpenWhisper main window",
  "windows": ["main"],
  "permissions": ["core:event:default"]
}
