#!/usr/bin/env sh
# Archived pre-rewrite launcher. It intentionally cannot start the obsolete
# Python application. Use `target/debug/openwhisper service start` in development.
echo "This runit launcher is archived; build the Rust/OpenTUI development bundle." >&2
exit 64
